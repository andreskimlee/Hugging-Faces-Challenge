{
  "name": "foobar-front",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "rollup": "^1.14.6",
    "rollup-plugin-node-resolve": "^5.0.1",
    "rollup-plugin-terser": "^5.0.0"
  },
  "scripts": {
    "prebuild": "rm -rf dist/* && rm -rf build/* && mkdir dist && mkdir build",
    "build:dev": "rollup -c",
    "grunt:watch": "cd ../grunt && grunt watch",
    "watch": "rollup -c -w"
  },
  "author": "",
  "license": "ISC"
}
